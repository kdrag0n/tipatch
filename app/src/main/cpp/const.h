#pragma once

namespace comp {
    const auto gzip       = 0;
    const auto lz4        = 1;
    const auto lzo        = 2;
    const auto xz         = 3;
    const auto bzip2      = 4;
    const auto lzma       = 5;
    const auto unknown    = 6;
}