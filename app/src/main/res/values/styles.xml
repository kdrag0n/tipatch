<resources>
    <style name="AppTheme" parent="@android:style/Theme.Material">
        <item name="android:colorPrimary">@color/action_dark</item>
        <item name="android:colorPrimaryDark">@color/status_dark</item>
        <item name="android:colorAccent">@color/accent</item>

        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:popupTheme">@style/MenuTheme</item>
    </style>

    <style name="MenuTheme" parent="@android:style/Theme.Material">
        <item name="android:colorBackground">@color/background_dark</item>
    </style>
    
    <style name="DialogTheme" parent="@android:style/Theme.Material.Dialog.Alert">
        <item name="android:background">@color/background_dark</item>
    </style>
</resources>
